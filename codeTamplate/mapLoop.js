const numArray = [0, 5, 20, 10, 45]

// * return from the function eace number with plus 5
function mapNumbars(array) {
  //   return the array gives with + 5 each
}

function mapNumbarsSolution(array = []) {
  let filterArray = array.map((elem) => {
    return elem + 5;
  });
  return filterArray;
}

arry = [5, 10, 25, 15, 50]
